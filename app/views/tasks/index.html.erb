<ul>
  <% @tasks.each do |task| %>
      <li><%= task.title %> </li>
      <li><%= task.details %> </li>
      <%= link_to task.title, task_path(task.id) %>
  <% end %>
  <%= link_to "Add a task", tasks_new_path %>
</ul>
