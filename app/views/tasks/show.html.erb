<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<%= link_to "Edit", edit_path(@task) %>

<%= link_to "Delete",
    delete_path(@task),
    method: :delete,
    data: { confirm: "Are you sure?" } %>

<%= link_to "Back", tasks_path %>


